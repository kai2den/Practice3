name: My workflow
on:
  push:
    branches:
      - main
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: action1
        run: touch myfile.txt

      - name: action2
        run: ls -la 
         